import { makeStyles } from '@material-ui/styles';

export default makeStyles((theme) => ({
    container: {
        padding: '40px 0px'
    },
    title: {
        fontFamily: 'LibreBaskerville',
        fontWeight: '700',
        padding: '20px',
    }
}));